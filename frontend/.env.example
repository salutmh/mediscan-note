# frontend 환경변수 - .env.development / .env.production 으로 복사해서 사용
# 로컬 개발 기본 포트는 8010 이다 (Windows 에서 8000 이 예약 범위에 걸려 WinError 10013 이 나는
# 경우가 있어 8010 으로 통일했다). 백엔드도 --port 8010 으로 띄운다.
# 배포 주소는 .env.production 에서 이 값만 바꾼다.
VITE_API_BASE=http://localhost:8010/api
